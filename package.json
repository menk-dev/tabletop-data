{
  "name": "treasureboard-data",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Daily ingest of Foundry VTT PF2e equipment into a slim, self-contained search bundle (equipment.json + deduped icons) published as a GitHub Release asset.",
  "scripts": {
    "ingest": "tsx ingest.ts",
    "test": "node --import tsx --test tests/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "sanitize-html": "^2.13.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/sanitize-html": "^2.13.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
